<h1>Create a Figure</h1>

<form action="/figures" method="post">

  Name: <input type="text" name="figure[name]" id="figure_name" placeholder="Name">
  <br>
  Titles: <br>
  <% @titles.each do |t| %>
    <input type="checkbox" value="<%= t.id %>" id="title_<%= t.id %>" name="figure[title_ids][]">
      <%= t.name %>
    </input>
  <% end %>
  <br>
  <br>

  Or, you can add a new title:<br>
  <input type="text" id="new_title" name="title[name]">
  <br>
  <br>


  Landmarks:<br>
  <% @landmarks.each do |l| %>
    <input type="checkbox" value="<%= l.id %>"
    name="figure[landmark_ids][]" id="landmark_<%= l.id %>">
      <%= l.name %>
    </input>
  <% end %>
  <br>
  <br>


  Or, you can add a new landmark:<br>
  <input type="text" name="landmark[name]" id="new_landmark">
  <br>
  <input type="submit" value="Create New Figure" id="Create New Figure" name="Create New Figure">
</form>
